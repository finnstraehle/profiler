<div class="card-saved">
  <div class="card-saved-content">
    <h4><%= resume.name %></h4>
    <div class="d-flex">
      <p><%= resume.company %></p>
      <p>&nbsp– <%= resume.created_at.strftime('%d/%m/%Y') %></p>
    </div>
  </div>
  <div class="card-saved-buttons">
    <%= link_to "<i class='fa-solid fa-download'></i>".html_safe, resume_path(resume, format: :pdf), class: "mx-3" %>
    <%= link_to "<i class='fa-regular fa-trash-alt'></i>".html_safe, resume_path(resume), data: { turbo_method: :delete, turbo_confirm: "Are you sure that you want to delete this Resume?"} %>
  </div>
</div>

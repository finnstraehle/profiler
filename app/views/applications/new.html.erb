<div class="container-l">
  <div class="header-page">
    <h1>New Application</h1>
  </div>
  <%= render "shared/back_button" %>  <div class="sheet-application">
    <%= simple_form_for(@application) do |f| %>
      <%= f.input :company %>
      <%= f.input :link %>
      <%= f.input :status, collection: Application::STATUS %>
      <%= f.input :notes, :input_html => { :rows => 6 } %>
      <%= f.input :resume_id, collection: @resumes %>
      <%= f.input :cover_letter_id, collection: @cover_letters %>
      <div class="d-flex" style="margin-top: 50px">
        <%= f.button :submit, class:"button", style:"margin: 0 auto" %>
      </div>
    <% end %>
  </div>
</div>
